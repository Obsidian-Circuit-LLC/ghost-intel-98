# W5 — OSINT consolidation: desktop declutter + Access-menu flyout + compact Toolkit — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`). Implementers run SEQUENTIALLY.

**Goal:** Remove OSINT tool icons from the desktop (keep only the OSINT Toolkit launcher), turn the Access-menu "OSINT Toolkit" entry into a one-hop flyout of the grouped OSINT tools, and make the Toolkit window compact.

**Architecture:** Reuse the existing Games-submenu flyout pattern in `AccessMenu.tsx` and the `buildOsintDirectory` grouping so the flyout mirrors the Toolkit window with zero duplication; drop two desktop entries; change one registration's default size.

**Tech Stack:** React renderer, zustand, TS strict, Vitest, Win98 styling.

## Global Constraints

- Commit as `onna-bugeisha-dev-team <dev@onna-bugeisha.org>`; NEVER any AI-identity trailer.
- Do NOT stage: `pnpm-lock.yaml`, `resources/satellites/active-snapshot.tle`, `native/dcs98-confine/Cargo.lock`.
- `pnpm typecheck` clean; suite green (2,469 baseline on main after W7).
- **ADHD-UX** (`ghostexodus-adhd-ui`): the flyout is ONE hop — no nested sub-submenus; grouped headings + a flat list of big, clickable tool rows; immediate launch on click. This should REDUCE friction, not add it.
- Safe because W1 landed: Hosts/News/Camera now degrade gracefully when launched standalone (error boundary + no-stream entries), so the flyout may list all 10 OSINT tools.

## Reference (verify against live code)

- Desktop icons (hardcoded): `src/renderer/shell/Desktop.tsx:14-25` (`desktopShortcutDefaults`) — OSINT entries `searchlight:22`, `socmint:23`, `osint-toolkit:24`. This is the ONLY desktop source.
- Access menu: `src/renderer/shell/AccessMenu.tsx`. Flat items from `settings.shortcuts`, rendered `:66-86`. **Games submenu (the flyout pattern to clone):** data `:20-26` (`GAMES`, `GAME_TARGETS`), flat-filter `:37-39`, flyout render `:88-121` (hover `position:relative` wrapper + `left:100%` panel), launch helper `openModule :41-45`. Single OSINT Toolkit entry to replace: `:122-134`.
- Grouping (reuse): `buildOsintDirectory(listModules())` — `src/renderer/modules/osint-toolkit/directory.ts:45-62`, priority `:16` (`['Social Media','Geospatial','Identity','Network / Recon']`). `listModules` from `src/renderer/state/registry.ts`.
- Toolkit window size: `src/renderer/modules/register-builtins.tsx:244` (currently `defaultWidth:760, defaultHeight:560`). Size resolution `src/renderer/state/store.ts:102-103`.

---

### Task 1: Remove OSINT tool icons from the desktop

**Files:** `src/renderer/shell/Desktop.tsx`, test

**Interfaces:**
- Produces: `desktopShortcutDefaults` no longer contains `searchlight` or `socmint`; `osint-toolkit` remains as the single OSINT desktop entry point. No other desktop change.

- [ ] **Step 1: Write the failing test** — `desktopShortcutDefaults` (or the rendered desktop icon set) contains `osint-toolkit` but NOT `searchlight`/`socmint`.
- [ ] **Step 2: Run, verify fails.**
- [ ] **Step 3: Implement** — remove the `searchlight` and `socmint` entries (`Desktop.tsx:22-23`); keep `osint-toolkit`.
- [ ] **Step 4: Targeted tests + typecheck green.**
- [ ] **Step 5: Commit** (`feat(desktop): remove OSINT tool icons; keep the OSINT Toolkit launcher`).

### Task 2: Access-menu "OSINT Toolkit" flyout

**Files:** `src/renderer/shell/AccessMenu.tsx`, test

**Interfaces:**
- Produces: an `OSINT_TARGETS` set of the 10 osint module keys (derive from `listModules().filter(m => m.category === 'osint')`); the flat-items filter (`:37-39`) also excludes `OSINT_TARGETS` (so eyespy/geoint/searchlight/socmint stop appearing as flat rows); the single OSINT Toolkit entry (`:122-134`) becomes a flyout cloned from the Games block (`:88-121`): a new `osintOpen` hover state, `position:relative` wrapper, `left:100%` panel populated from `buildOsintDirectory(listModules())` — group headings + one row per tool, each calling `openModule(tool.key, tool.title)`. One hop, grouped.

- [ ] **Step 1: Write the failing test** — the Access-menu flat items exclude the OSINT targets; the OSINT flyout renders grouped headings + a row per tool in `buildOsintDirectory` order; clicking a tool row opens that module (`open`/`openModule` called with the tool key). Mirror the existing Games-submenu test if one exists.
- [ ] **Step 2: Run, verify fails.**
- [ ] **Step 3: Implement** the flyout (clone the Games block; reuse `buildOsintDirectory`). Keep it one hop (no nested sub-submenu).
- [ ] **Step 4: Targeted tests + typecheck green.**
- [ ] **Step 5: Commit** (`feat(access): OSINT Toolkit flyout submenu of the grouped OSINT tools`).

### Task 3: Compact Toolkit window

**Files:** `src/renderer/modules/register-builtins.tsx`, test

**Interfaces:**
- Produces: the `osint-toolkit` registration uses a compact `defaultWidth`/`defaultHeight` (~360×470) matching the operator's screenshot, instead of 760×560.

- [ ] **Step 1: Write the failing test** — the registered `osint-toolkit` descriptor's `defaultWidth`/`defaultHeight` are the new compact values (assert the exact numbers).
- [ ] **Step 2: Run, verify fails.**
- [ ] **Step 3: Implement** — change the two numbers at `register-builtins.tsx:244`.
- [ ] **Step 4: Targeted tests + typecheck green.**
- [ ] **Step 5: Commit** (`feat(osint-toolkit): compact default window size`).

### Task 4: Full green

- [ ] **Step 1:** `pnpm test` + `pnpm typecheck` green; fix any fallout (e.g. a test asserting the old desktop/Access set, or the reachability audit).
- [ ] **Step 2: Commit** if needed (`test(osint): full-suite green after consolidation`).

## Verification

- Desktop shows the OSINT Toolkit icon but not the individual OSINT tool icons.
- Access-menu "OSINT Toolkit ▸" opens a one-hop grouped flyout; eyespy/geoint/searchlight/socmint no longer appear as flat rows; each flyout row launches its module.
- Toolkit window opens compact. `pnpm test` + `pnpm typecheck` green.
