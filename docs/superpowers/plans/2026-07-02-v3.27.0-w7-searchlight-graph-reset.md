# W7 — Searchlight: reset the relationship-tree graph + re-import — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`). Implementers run SEQUENTIALLY.

**Goal:** Let the user wipe the entire Searchlight relationship graph for the active case (with a confirm) and re-import it, without leaving stale selection state or touching other cases.

**Architecture:** One new store action `clearGraph(caseId)` (persisted through the existing debounced save) + a confirm-gated toolbar button that also resets the panel's local view state. The re-import path already exists (`importResultsToGraph`).

**Tech Stack:** React renderer, zustand store, TS strict, Vitest.

## Global Constraints

- Commit as `onna-bugeisha-dev-team <dev@onna-bugeisha.org>`; NEVER any AI-identity trailer.
- Do NOT stage: `pnpm-lock.yaml`, `resources/satellites/active-snapshot.tle`, `native/dcs98-confine/Cargo.lock`.
- `pnpm typecheck` clean; suite green (2,457 baseline on main after W2).
- **Per-case scoped only** — key everything off `activeCaseId`; never clear graph data globally. Per `searchlight-panel-unmount-state`: GraphView derives nodes/edges fresh from `activeCase` and holds no cross-case pointer — keep it that way; the reset must clear the panel's LOCAL `useState` (selected/connecting/editingNote/showAddMenu) so no id survives referencing a deleted node. Do NOT touch the store's `selectedJobId` pointer (used by the Sweep panel).

## Reference (verify against live code)

- Graph view: `src/renderer/modules/searchlight/panels/GraphView.tsx` (toolbar ~442-519, pan/zoom RESET ~510-516 is view-only, DELETE ~490-500, danger style `sl-sweep-btn-danger` ~491, local `useState` ~171-184, `nodes`/`edges` derived ~190-191).
- Store: `src/renderer/modules/searchlight/store.ts` (graph mutators ~264-313, `mutateCaseAndSave` ~86-100, `scheduleSave` ~29-37). Types `graphNodes`/`graphEdges` on `SearchlightCase` — `src/shared/searchlight/types.ts:97-98`.
- Existing re-import (reusable): `importResultsToGraph` `GraphView.tsx:345-413`, Import panel ~521-634.

---

### Task 1: `clearGraph(caseId)` store action

**Files:** `src/renderer/modules/searchlight/store.ts`, `test/searchlight-*.test.ts` (find the store test)

**Interfaces:**
- Produces: `clearGraph(caseId: string): void` on the Searchlight store — sets that case's `graphNodes: []`, `graphEdges: []`, `updatedAt: Date.now()` via `mutateCaseAndSave` (so the debounced `saveCase` persists it). Touches no other case.

- [ ] **Step 1: Write the failing test** — with two cases each having nodes/edges, `clearGraph(caseA)` empties caseA's `graphNodes`/`graphEdges` and leaves caseB untouched; the case's `updatedAt` advances; the save path is scheduled (mirror how the existing mutator tests assert persistence).
- [ ] **Step 2: Run, verify fails.**
- [ ] **Step 3: Implement** `clearGraph` mirroring the existing graph mutators (declare in the state interface near the other graph mutators; implement via `mutateCaseAndSave`).
- [ ] **Step 4: Targeted tests + typecheck green.**
- [ ] **Step 5: Commit** (`feat(searchlight): clearGraph store action to wipe a case's relationship graph`).

### Task 2: "Clear Graph" toolbar button + confirm + local-state reset

**Files:** `src/renderer/modules/searchlight/panels/GraphView.tsx`, test

**Interfaces:**
- Produces: a "Clear Graph" toolbar button (destructive `sl-sweep-btn-danger` style, near the existing DELETE/RESET buttons) that, on a confirm, calls `store.clearGraph(activeCaseId)` then resets the panel's local view state (`setSelected(null)`, `setConnecting(null)`, `setEditingNote(null)`, `setShowAddMenu(false)`). Disabled/hidden when there are zero nodes. The existing Import Results panel already provides re-import (no new code); optionally add a "clear then re-import current case" convenience calling `clearGraph` then `importResultsToGraph(activeCaseId, new Set(), true)`.

- [ ] **Step 1: Write the failing test** — clicking "Clear Graph" and confirming calls `clearGraph(activeCaseId)` and resets local selection state; cancelling the confirm does nothing; the button is disabled/absent with zero nodes.
- [ ] **Step 2: Run, verify fails.**
- [ ] **Step 3: Implement** the button + confirm + local reset. Confirm must be explicit (destructive). Keep it per-case (`activeCaseId`).
- [ ] **Step 4: Targeted tests + typecheck green.**
- [ ] **Step 5: Commit** (`feat(searchlight): confirm-gated "Clear Graph" button with local-state reset`).

### Task 3: Full green

- [ ] **Step 1:** `pnpm test` + `pnpm typecheck` green; fix any fallout.
- [ ] **Step 2: Commit** if needed (`test(searchlight): full-suite green after graph reset`).

## Verification

- `clearGraph` empties only the active case's graph; other cases untouched; persisted.
- Button confirms before wiping; resets local selection; re-import rebuilds. `pnpm test` + `pnpm typecheck` green.
