# v3.41.0 — Compact Number Muncher + 512 MB viewer cap — Implementation Plan

> Executed inline (controller, TDD). Layout reflow verified with jsdom structure tests **and** a headless Playwright computed-style probe (the width/no-side-column truth a text review can't see). Small, coupled — 2 tasks.

**Goal:** Shrink Number Muncher to ~380×580 (slim rail, no side column) and raise the My-Documents viewer cap 64 MB → 512 MB.

**Architecture:** Layout-only shell reflow (Memory→row, History→toggle drawer, Info→status footer, drop the grid's 3rd track); one constant for the cap. No engine/IPC/store-logic changes.

## Global Constraints
- No new dependency, no new egress. My-Documents files stay encrypted at rest; viewer decrypts in-process (no OS handoff).
- Memory-scope rule preserved: MC/MR/MS/M+/M- enabled only in Standard/Scientific, disabled elsewhere.
- Every calculator engine + keypad untouched; all 7 modes keep working.
- Commit author `onna-bugeisha-dev-team`; no AI trailers; explicit-path `git add`.

---

### Task 1: Raise the viewer cap to 512 MB

**Files:** Modify `src/main/documents/store.ts`; Test `test/documents-open-export.test.ts`.

- Change `MAX_VIEW_BYTES` from `64 * 1024 * 1024` to `512 * 1024 * 1024`. Message text auto-derives the MB from the constant — no string edit needed.
- Update the boundary test: truncate to `512 * 1024 * 1024 + 1` (still rejected `/too large/i`); add a positive case that a ~108 MB-class file (or any file under the cap) reads its bytes fine.
- Run `pnpm test documents-open-export`; commit.

### Task 2: Compact Number Muncher shell

**Files:** Modify `src/renderer/modules/number-muncher/NumberMuncherModule.tsx`, `src/renderer/styles/theme.css`, `src/renderer/modules/register-builtins.tsx`; Test `test/number-muncher-module.test.tsx`.

**JSX (`NumberMuncherModule.tsx`):**
- `.ga98-calc` root: remove the `.ga98-calc-side` block entirely.
- Add a `showHistory` boolean state (default false).
- In `.ga98-calc-main`: wrap the display in a `.ga98-calc-topbar` with a History toggle button (`onClick` flips `showHistory`, `aria-pressed`).
- Add a `.ga98-calc-mem-row` (the 5 mem buttons + inline `M = {memory}`), moved out of the old panel, keeping `disabled={!memActive}` + title.
- After the keypad, add a `.ga98-calc-statusbar` one-liner: `{modeLabel} · 64-bit · {std.error ? 'Error' : 'Ready'}` (+ last result when not '—').
- When `showHistory`, render a `.ga98-calc-history-drawer` (absolutely positioned inside `.ga98-calc-main`) holding the existing history list + Clear + a close button.

**CSS (`theme.css`):**
- `.ga98-calc` grid `auto 1fr 200px` → `auto 1fr`.
- `.ga98-calc-main` gets `position: relative` (drawer anchor).
- Add `.ga98-calc-topbar` (flex: display grows, toggle pinned right), `.ga98-calc-mem-row` (flex row, slim buttons + mono readout), `.ga98-calc-statusbar` (one line, small, muted), `.ga98-calc-history-drawer` (absolute, inset, `z-index`, white scroll list, Clear/close).
- Remove/retire the now-unused `.ga98-calc-side` / panel rules that no longer apply (keep `.ga98-calc-mem` button sizing; keep history list styles reused by the drawer).

**Registration (`register-builtins.tsx`):** `defaultWidth: 760 → 380`, `defaultHeight: 620 → 580`.

**Tests (`number-muncher-module.test.tsx`):**
- 7-mode rail still renders (unchanged assertion).
- No `.ga98-calc-side` in the DOM.
- `.ga98-calc-mem-row` has 5 buttons; all disabled in Programmer, enabled in Standard/Scientific (preserve the v3.40.0 rule).
- History drawer hidden by default; clicking the toggle shows `.ga98-calc-history-drawer`; clicking again hides it.
- Status footer text contains the active mode label.
- Existing "2 + 3 = 5" drive test still passes.

Run `pnpm test number-muncher-module`; commit.

## Verification (controller, before merge)
- `pnpm test` full + `pnpm typecheck` green.
- **Headless layout probe** (Playwright, one throwaway harness): render each of the 7 modes at the 380 px shell width; assert the root has no horizontal overflow (`scrollWidth <= clientWidth + tolerance`) and no `.ga98-calc-side`. Confirms the reflow fits every mode.
- Grep packaged asar for `ga98-calc-mem-row` / `ga98-calc-history-drawer` after build.
