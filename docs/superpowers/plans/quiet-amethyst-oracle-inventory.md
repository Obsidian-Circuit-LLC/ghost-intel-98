# QUIET AMETHYST — rendered-contrast oracle inventory

Generated by `test/theme-rendered-contrast.test.ts` (do not hand-edit — rerun the oracle).

- modules rendered: **41**
- modules with flagged sites: **8**
- total flagged sites: **24**
- coverage gaps (could not render): **1**
- exemptions in force: `.ga98-card`

## Flagged sites by module

### eyespy — EyeSpy (2)

**Contrast:**

- `div` ratio **4.22** — fg `rgb(119, 119, 119)` on bg `rgb(17,17,17)` (11px) — "Add new feed"
- `div` ratio **4.22** — fg `rgb(119, 119, 119)` on bg `rgb(17,17,17)` (10px) — "empty"

### ai-assistant — Q (1)

**Contrast:**

- `div` ratio **4.05** — fg `rgb(138, 134, 160)` on bg `rgb(44,41,56)` (11px) — "Set a provider in Settings, optionally pick a case for conte"

### geoint — GeoINT (8)

**Contrast:**

- `span` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (10px) — "0 high-severity events in view"
- `span` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (11px) — "No events in view."
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (10px) — "Source: located feed + threat-layer events in view. Threat l"
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (11px) — "No corroborated clusters in view."
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (10px) — "Source: corroborate() — count of distinct other sources agre"
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (10px) — "Network off — imagery disabled."
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (11px) — "No events in view."
- `p` ratio **3.95** — fg `rgb(107, 118, 136)` on bg `rgb(17,22,31)` (10px) — "Source: same feed + threat-layer items as the Events list (t"

### chess — Chess (6)

**Contrast:**

- `span` ratio **1.37** — fg `rgb(255, 255, 255)` on bg `rgb(240,217,181)` (38px) — "♙"
- `span` ratio **1.37** — fg `rgb(255, 255, 255)` on bg `rgb(240,217,181)` (38px) — "♘"
- `span` ratio **1.37** — fg `rgb(255, 255, 255)` on bg `rgb(240,217,181)` (38px) — "♕"
- `span` ratio **1.37** — fg `rgb(255, 255, 255)` on bg `rgb(240,217,181)` (38px) — "♗"
- `span` ratio **1.37** — fg `rgb(255, 255, 255)` on bg `rgb(240,217,181)` (38px) — "♖"

**Light islands:**

- `div` bg `rgb(240, 217, 181)` (lum 0.715, area 2704px²)

### help — RTFM (1)

**Contrast:**

- `a` ratio **1.32** — fg `rgb(0, 0, 170)` on bg `rgb(26,24,34)` (12px) — "mailto:contact.bugreport@qwamos.com"

### invoices — Ghost Ledger 98 (2)

**Contrast:**

- `li.ga98-invoices-empty` ratio **1.11** — fg `rgb(14, 12, 20)` on bg `rgb(26,24,34)` (11px) — "No invoices yet."
- `div.ga98-invoices-placeholder` ratio **1.07** — fg `rgb(14, 12, 20)` on bg `rgb(26,15,46)` (11px) — "Select an invoice or create a new one."

### number-muncher — Number Muncher (3)

**Contrast:**

- `span.ga98-calc-status-mode` ratio **1.11** — fg `rgb(14, 12, 20)` on bg `rgb(26,24,34)` (11px) — "Standard"
- `span` ratio **1.11** — fg `rgb(14, 12, 20)` on bg `rgb(26,24,34)` (11px) — "64-bit"
- `span.ga98-calc-status-result` ratio **1.11** — fg `rgb(14, 12, 20)` on bg `rgb(26,24,34)` (11px) — "Ready"

### pdf-signer — PDF Signer (1)

**Light islands:**

- `canvas.ga98-sig-canvas` bg `rgb(255, 255, 255)` (lum 1, area 23124px²)

## Coverage gaps (modules that could not be server-rendered)

- **doc-viewer** (Document Viewer): Cannot read properties of undefined (reading 'toLowerCase')
