# QUIET AMETHYST — rendered-contrast oracle inventory

Generated by `test/theme-rendered-contrast.test.ts` (do not hand-edit — rerun the oracle).

- modules audited: **38**
  - fully rendered (data surface present): **34**
  - empty-state-only (data surfaces UNAUDITED): **4**
- modules with flagged sites: **0**
- total flagged sites: **0**
- coverage gaps (could not render an auditable surface): **4**
- exemptions in force: `.ga98-card`, `.ga98-chess-board`, `.ga98-sig-canvas`, `.ga98-mine-grid`, `.ga98-geo-map`, `.ga98-geo-cat-swatch`, `.ga98-geo-threat`

> **What this oracle proves — and does not.** It is a strong regression guard for the
> rendered CHROME and EMPTY-STATE surface of every built-in module under QUIET AMETHYST
> (real Chrome cascade + `var()` resolution, WCAG contrast, light-island + gradient +
> alpha/opacity compositing). It is NOT a proof of total coverage: the empty-state-only
> modules above rendered their shell but not their populated rows/cells (those are built
> by effects `renderToStaticMarkup` never runs), and the walker has disclosed latent gaps
> (raster background-image islands, `::before`/`::after` pseudo-element text, SSR
> layout-area dependence — see `test/helpers/chrome-computed-style.ts`).

## Empty-state-only modules (audited shell; data surfaces UNAUDITED)

- **geoint** (GeoINT): empty data container(s): ul, tbody, .ga98-list
- **markets** (Markets): empty data container(s): ul, .ga98-list
- **solitaire** (Solitaire): empty data container(s): .ga98-card, .ga98-card-corner, .ga98-card-pip
- **number-muncher** (Number Muncher): empty data container(s): .ga98-calc-status-result

## Flagged sites by module

_No flagged sites — the oracle is green._
## Coverage gaps (modules that could not be server-rendered)

- **settings** (Settings): non-substantive static render (1 element: "Loading…") — real surface is gated behind an effect-driven data/PIN load that renderToStaticMarkup does not run; not exercised by this oracle
- **bookmarks** (Bookmarks): non-substantive static render (1 element: "Loading…") — real surface is gated behind an effect-driven data/PIN load that renderToStaticMarkup does not run; not exercised by this oracle
- **journal** (Journal Jots): non-substantive static render (1 element: "Opening your journal…") — real surface is gated behind an effect-driven data/PIN load that renderToStaticMarkup does not run; not exercised by this oracle
- **minds-eye** (Mind's Eye): non-substantive static render (1 element: "Loading the memory graph…") — real surface is gated behind an effect-driven data/PIN load that renderToStaticMarkup does not run; not exercised by this oracle
